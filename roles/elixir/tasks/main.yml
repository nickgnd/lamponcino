---
- name: Install elixir
  become: yes
  apt:
    state: present
    name:
      - elixir
  tags: [elixir, packages]

- name: Register elixir version
  shell: elixir --version
  register: elixir_version
  tags: elixir

- name: Print elixir version
  debug:
    var: elixir_version
  tags: elixir
